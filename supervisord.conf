print(default_api.natural_language_write_file(language = "ini", path = "supervisord.conf", prompt = "Modify the supervisord.conf file:\n1.  Make sure that the `[supervisord]` section exists and the `nodaemon` option is `true`.\n2.  Add a `[program:x11vnc]` section where the command to run is `/usr/bin/x11vnc -display :99 -rfbport 5900`.\n3.  Add a `[program:websockify]` section where the command to run is `/usr/bin/websockify -v --web=/usr/share/novnc 6080 localhost:5900`."))
